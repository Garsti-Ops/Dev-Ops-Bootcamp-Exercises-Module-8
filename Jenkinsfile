pipeline {
    agent {
        any
    }
    stages {
        stage("Increment Version") {
            steps {
                sh 'docker --version'
                sh ''
            }
        }
        stage("Tests") {

        }
        stage("Build Docker Image and push to Repository") {

        }
    }

}